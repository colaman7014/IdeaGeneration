name: idea_generation
description: 新聞到點子生成器 - 讓專業人士在 3 分鐘內從新聞產生商業想法
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # HTTP 請求
  http: ^1.1.0
  # 狀態管理
  provider: ^6.1.1
  # 本地儲存
  shared_preferences: ^2.2.2
  # Markdown 渲染
  flutter_markdown: ^0.6.18
  # 圖示
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
